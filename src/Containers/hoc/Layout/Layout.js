import React, {Component} from 'react';

class Layout extends Component {
    render() {
        return {
            // Add toolbar with navigationItems and navItem's
            // add <Logo /> with some logo I create online
            // add sideDrawer with toggle 3 stack lines
            // needs state for open/close sideDrawer and possibly chow/hide NavItem's
        };
    }
};

export default layout;
